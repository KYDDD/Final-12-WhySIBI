# 장애 유형별 복구 시나리오

---

## 1. 전원, 하드웨어 장애

전원 케이블 등 외부 전원 공급 상태를 먼저 점검한다.  
외부 전원이 정상이면 본체 전원 버튼으로 재가동을 시도한다.  
재가동 후에도 부팅되지 않으면 비프음/LED 오류코드를 확인하여 이상 부위를 파악한다.  
자체 조치가 어려운 하드웨어 결함으로 판단되면 제조사 또는 유지보수 업체에 연락한다.  
서비스 중단이 예상되는 경우 사용자에게 장애 공지(이메일, 메신저 등)를 즉시 발송한다.  
예비 장비 또는 백업 서버가 있는 경우, 임시 서비스 전환을 검토한다.  
하드웨어 교체 후 재부팅하여 정상 마운트 되는지 확인하고, 서비스 재개 여부를 검증한다.

---

## 2. 네트워크 접속 불가

사용자 PC, 부서, 사무실 전체 중 어느 범위에서 접속 장애가 발생했는지 범위를 먼저 파악한다.  
NAS 본래의 네트워크 포트 상태를 확인한다.  
스위치/공유기 등 중간 네트워크 장비의 전원 및 포트 Link 상태를 점검한다.  
NAS에 할당된 IP, 게이트웨이, DNS 설정에 변경 사항이 없는지 확인한다.  
케이블 불량이 의심되면 예비 케이블로 교체하거나 다른 포트로 연결해 본다.  
사내망 자체 장애인 경우 네트워크 담당자와 협업하여 스위치 재기동 또는 설정 복원을 진행한다.  
복구 후 서비스가 정상 구동 중인지 재확인한다.

---

## 3. 운영체제(OS) 장애 / Windows Update 오류

서버에 콘솔 또는 원격으로 접속을 시도하여 응답 여부를 확인한다.  
접속이 가능하면 이벤트 뷰어(Event Viewer)에서 오류 로그를 확인하고 원인을 파악한다.  
최근 Windows Update 이력을 확인하고, 문제 업데이트가 있으면 제거한다.  
`설정 > 업데이트 및 보안 > 업데이트 기록 > 업데이트 제거` 경로로 롤백을 진행한다.  
업데이트 제거 후에도 부팅이 불가한 경우 안전 모드(Safe Mode)로 부팅을 시도한다.  
안전 모드에서도 복구가 어려우면 서버 스냅샷 또는 백업 이미지를 이용해 복원한다.  
복원 후 서비스 기동 및 정상 동작 여부를 확인하고, 해당 업데이트는 설치 제외 처리한다.

---

## 4. DB 사용 시스템 장애

DB를 사용하는 여러 서비스가 동시에 오류를 보일 경우 DB 서버 장애를 우선 의심한다.  
DB 서버의 서비스 프로세스 상태를 확인한다. (`systemctl status mysql` 또는 SQL Server 서비스 확인)  
에러 로그를 분석하여 오류 원인을 파악한다. (디스크 용량 부족, 프로세스 충돌, 권한 오류 등)  
원인이 파악되면 해당 문제를 먼저 해결한 뒤 DB 서비스를 재기동한다.  
단순 재기동으로 복구되지 않을 경우 최신 DB 백업본을 이용해 복원을 진행한다.  
백업 복원 후에는 트랜잭션 로그를 적용하여 데이터 손실을 최소화한다.  
DB 복구 완료 후 연결된 애플리케이션 서버를 순차적으로 재기동하고 정상 동작을 확인한다.  
JTY 관리 서버에 오류가 발생한 경우, 서비스 재기동 후 클라이언트 재접속을 확인한다.

---

## 5. 전력 공급 장애 / 화재

전력 공급 이상 발생 시 UPS 상태를 즉시 확인하고 배터리 잔량을 파악한다.  
UPS 배터리가 소진되기 전에 서버 셧다운 절차를 시작하여 데이터 손상을 방지한다.  
화재 발생 시에는 인명 안전을 최우선으로 하여 즉시 대피하고 소방서(119)에 신고한다.  
전력이 복구되면 아래 순서에 따라 서버를 순차적으로 재기동한다.

> **재기동 순서:** 네트워크 장비 → 스토리지 → 가상화 호스트 → DB 서버 → 인증 서버 → 애플리케이션 서버 → 웹 서버

비정상 전원 차단으로 인한 파일시스템 손상이 의심되면 `fsck` 등 디스크 검사를 먼저 수행한다.  
DB 서버는 비정상 종료 복구 절차에 따라 트랜잭션 로그 롤백을 적용한 뒤 기동한다.  
전체 서비스 정상화 후 시설팀과 함께 전력 설비 점검을 완료하고 재발 방지 조치를 취한다.

---

## 6. 사이버 공격 (랜섬웨어 감염)

랜섬웨어 감염이 의심되는 즉시 해당 시스템의 네트워크 케이블을 물리적으로 차단하여 확산을 막는다.  
감염 범위를 조사하고 영향을 받은 시스템 목록을 작성한다.  
백업 서버가 동일 네트워크에 있는 경우, 백업 서버도 즉시 격리하여 2차 감염을 방지한다.  
감염 경로(이메일, 외부 접속, 취약점 등)를 파악하고 해당 경로를 차단한다.  
감염된 시스템은 OS를 완전히 초기화(포맷 후 재설치)하고 최신 보안 패치를 적용한다.  
오프라인(에어갭) 백업의 무결성을 해시값으로 검증한 뒤, 감염 이전 시점의 백업으로 데이터를 복원한다.  
복원 완료 후 전체 시스템에 대해 악성코드 스캔을 실시하고 이상이 없음을 확인한다.  
전체 계정 비밀번호를 강제 변경하고 다중인증(MFA) 적용을 확대한다.  
KISA 인터넷침해대응센터(118)에 사이버 침해 신고를 진행한다.  
복구 완료 후 보안 감사를 실시하고 재발 방지 대책을 마련한다.

---

## 참고 — 복구 우선순위 및 목표

| Level | 장애 유형 | 목표 복구 시간 (RTO) | 발생 가능성 |
|:---:|:---|:---:|:---:|
| 1 | 단일 서버 장애 (OS 오류, Update) | 1시간 이내 | 1회/3년 |
| 2 | DB 사용 시스템 장애, JTY 서버 오류 | 2시간 이내 | 1회/5년 |
| 3 | 전력공급 장애, 화재 | 8시간 이내 | 1회/20년 |
| 4 | 네트워크 전체 장애 | 4시간 이내 | 1회/20년 |
| 5 | 사이버 공격 (랜섬웨어) | 72시간 이내 | 1회/20년 |
