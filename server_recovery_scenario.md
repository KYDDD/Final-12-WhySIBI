# 장애 유형별 복구 시나리오

---

## 1. 전원, 하드웨어 장애

먼저 전원 케이블이 잘 꽂혀 있는지 확인한다.  
케이블이 정상이면 전원 버튼을 눌러서 다시 켜본다.  
다시 켜도 부팅이 안 되면 삐 소리나 LED 표시등을 확인해서 어디가 문제인지 본다.  
혼자 고치기 어려울 것 같으면 제조사나 유지보수 업체에 바로 연락한다.  
서버가 오래 꺼져 있을 것 같으면 담당자나 사용자에게 미리 알린다.  
여분 서버가 있으면 그쪽으로 임시로 전환하는 것도 고려한다.  
하드웨어를 교체하고 나서 정상적으로 켜지는지, 서비스가 잘 되는지 확인한다.

---

## 2. 네트워크 접속 불가

내 컴퓨터만 안 되는지, 부서 전체가 안 되는지, 회사 전체가 안 되는지 먼저 확인한다.  
서버 뒷면의 랜 포트에 불이 들어오는지 확인한다.  
중간에 있는 스위치나 공유기 전원이 켜져 있는지 확인한다.  
서버 IP 주소나 게이트웨이 설정이 바뀐 게 없는지 확인한다.  
랜선이 문제일 수도 있으니 다른 랜선으로 바꿔보거나 다른 포트에 꽂아본다.  
회사 네트워크 자체가 문제면 네트워크 담당자한테 연락해서 같이 해결한다.  
다 고치고 나서 서비스가 잘 되는지 다시 한번 확인한다.

---

## 3. 운영체제(OS) 장애 / Windows Update 오류

서버에 원격으로 접속해보거나 직접 모니터를 연결해서 상태를 확인한다.  
접속이 되면 Windows 이벤트 뷰어에서 빨간 오류 표시가 있는지 확인한다.  
최근에 Windows Update를 했다면 그게 원인일 수 있으니 되돌려본다.  
`설정 > 업데이트 및 보안 > 업데이트 기록 > 업데이트 제거`에서 최근 업데이트를 제거한다.  
그래도 안 켜지면 안전 모드로 부팅해서 다시 시도해본다.  
안전 모드도 안 되면 백업해둔 스냅샷으로 복원한다.  
복원 후에는 서비스가 정상적으로 켜지는지 확인하고, 문제가 됐던 업데이트는 다시 설치 안 되도록 막아둔다.

---

## 4. DB 사용 시스템 장애

여러 서비스가 동시에 오류가 나면 DB 서버가 문제일 가능성이 높다.  
DB 서버에 접속해서 서비스가 켜져 있는지 확인한다.  
꺼져 있으면 서비스를 다시 켜본다.  
오류 로그 파일을 열어서 어떤 이유로 꺼졌는지 확인한다. (디스크가 꽉 찼거나, 프로그램이 충돌했거나 등)  
원인을 해결한 다음 DB를 다시 켠다.  
그래도 안 되면 전날 자동으로 만들어둔 백업으로 복원한다.  
DB가 살아나면 DB를 사용하는 다른 서버들도 하나씩 재시작한다.  
JTY 관리 서버에 오류가 생겼으면 서비스를 재시작하고 접속이 잘 되는지 확인한다.

---

## 5. 전력 공급 장애 / 화재

전기가 나갔으면 UPS(무정전 전원장치) 배터리가 얼마나 남았는지 확인한다.  
배터리가 다 떨어지기 전에 서버를 정상 종료해서 데이터가 손상되지 않게 한다.  
화재가 났으면 사람이 먼저다. 바로 대피하고 119에 신고한다.  
전기가 다시 들어오면 서버를 아래 순서대로 하나씩 켠다.

> **켜는 순서:** 네트워크 장비 → 스토리지 → DB 서버 → 나머지 서버들 → 웹 서버

갑자기 꺼진 경우에는 디스크가 손상됐을 수 있으니 켜기 전에 디스크 검사를 먼저 해본다.  
다 켜고 나서 서비스가 정상적으로 돌아가는지 확인한다.

---

## 6. 사이버 공격 (랜섬웨어 감염)

파일이 이상하게 암호화되거나 랜섬웨어 메시지가 뜨면 즉시 랜선을 뽑아서 다른 서버로 퍼지지 않게 한다.  
감염된 서버가 몇 대인지 파악한다.  
백업 서버도 같은 네트워크에 있으면 랜선을 뽑아서 백업까지 감염되는 걸 막는다.  
어떻게 감염됐는지 원인을 찾아서 그 경로를 차단한다. (이메일 첨부파일, 외부 접속 등)  
감염된 서버는 윈도우를 새로 설치하고 최신 보안 업데이트를 적용한다.  
네트워크에서 완전히 분리해서 보관해둔 백업(외장하드, 테이프 등)이 있으면, 그 백업 파일이 깨지지 않았는지 먼저 확인하고 복원한다. (백업 파일도 랜섬웨어에 감염됐을 수 있어서 확인이 필요함)  
복원 후에는 바이러스 검사를 전체 서버에 다 돌려본다.  
모든 계정 비밀번호를 바꾼다.  
KISA 인터넷침해대응센터(118번)에 신고한다.  
다 끝나면 어떻게 해서 감염됐는지 정리하고 같은 일이 반복되지 않도록 대책을 만든다.

---

## 참고 — 복구 우선순위 및 목표

| Level | 장애 유형 | 목표 복구 시간 | 발생 가능성 |
|:---:|:---|:---:|:---:|
| 1 | 단일 서버 장애 (OS 오류, Update) | 1시간 이내 | 1회/3년 |
| 2 | DB 관련 시스템 장애, JTY 서버 오류 | 2시간 이내 | 1회/5년 |
| 3 | 전력공급 장애, 화재 | 8시간 이내 | 1회/20년 |
| 4 | 네트워크 전체 장애 | 4시간 이내 | 1회/20년 |
| 5 | 사이버 공격 (랜섬웨어) | 72시간 이내 | 1회/20년 |
