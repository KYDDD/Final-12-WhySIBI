# Public FESP (bootcamp) API config used by the app.
# The Cloud Agent environment install step recreates .env.local with these values.
# Copy this file to .env.local for local development:  cp .env.example .env.local
NEXT_PUBLIC_API_URL=https://fesp-api.koyeb.app/market
NEXT_PUBLIC_CLIENT_ID=febc13-final12-emjf
